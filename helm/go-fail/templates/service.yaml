apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-fail.fullname" . }}
  labels:
    {{- include "go-fail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-fail.selectorLabels" . | nindent 4 }}